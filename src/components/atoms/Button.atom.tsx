import tw from 'twin.macro';

const Button = tw.button`py-3 px-11 bg-accent shadow-md hover:(bg-accent-light scale-105) text-textWhite font-semibold rounded-full transition-all [transition-duration: 300ms]`;

export default Button;
